spring:
  application:
    name: notification-service
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yml
        shared-configs:
          - data-id: shared-kafka.yml
            refresh: true
          - data-id: shared-jwt.yml
            refresh: true
