spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    # URL is set per-service in each service's application.yml (different DB name,
    # localhost on the host). Shared config carries only credentials + driver.
    username: root
    password: root
